# Build output
bin/
x64/
Release/
assets/textures/test
*.exe
*.obj
*.pdb
*.ilk
*.exp

# VS clutter
.vs/
*.user
*.suo
*.VC.db
*.VC.opendb

# Saves (world data — optional, exclude for clean repo)
saves/

# Dependencies (if vendored and large)
# vendor/  ← only ignore if deps are huge